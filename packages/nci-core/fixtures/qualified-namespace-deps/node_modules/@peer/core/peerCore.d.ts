/** Declarations for @peer/core; the package entry re-exports via `export *` only. */

export interface PeerOpts {
  readonly x: number;
}

export interface PeerOut<T> {
  readonly out: T;
}

/** Exercised through barrel entry: resolver lands on index.d.ts; symbol is indexed here. */
export type Box<T> = T | Promise<T>;
