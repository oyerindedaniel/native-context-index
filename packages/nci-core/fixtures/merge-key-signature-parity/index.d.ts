/** Two overloads of one name plus a value — ensures merged graph keeps overload rows distinct. */
export declare function mergedOverload(x: string): void;
export declare function mergedOverload(x: number): void;
export declare const afterOverload: boolean;
