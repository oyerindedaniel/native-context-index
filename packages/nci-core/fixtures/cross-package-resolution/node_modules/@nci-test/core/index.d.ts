/**
 * Internal interface not explicitly exported by the meta-package.
 */
interface InternalConfig {
    retries: number;
    timeout: number;
}

/**
 * The actual definition we want to resolve.
 */
export interface CoreService {
    readonly id: string;
    start(config: InternalConfig): void;
}

export type ServiceStatus = 'active' | 'inactive';
