import { PageEyebrow, PageLede } from "@/components/docs/page-eyebrow";
import { PrimerView } from "@/components/docs/primer-view";

import { metadataForDocsPath } from "@/lib/docs/page-metadata";

export const metadata = metadataForDocsPath("/docs/primer");


<PageEyebrow>Primer</PageEyebrow>

# Agent primer

<PageLede>
  This is the same text the MCP server returns at `nci://primer/agent` and `nci://primer/reference`. **Compact** is the workflow primer (fast path, tools, stubs, graph semantics). **Reference** is schema-only — SQLite tables and columns; read compact first.
</PageLede>

<PrimerView />
